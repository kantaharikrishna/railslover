<%= form_for(@memberimage) do |f| %>
  <% if @memberimage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@memberimage.errors.count, "error") %> prohibited this memberimage from being saved:</h2>

      <ul>
      <% @memberimage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Choose_image %>
    <%= f.file_field :image %>
  </div>
  <br/>
  <div class="field">
    
    <%= f.hidden_field :member_id, :value => current_member.id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
